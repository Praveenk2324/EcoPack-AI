# XGBoost hyperparameters
xgboost:
  n_estimators: 200
  max_depth: 8
  learning_rate: 0.1
  subsample: 0.8
  colsample_bytree: 0.8
  reg_alpha: 0.1
  reg_lambda: 1.0

# Train/test split
split:
  test_size: 0.2
  random_state: 42
